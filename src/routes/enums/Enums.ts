export enum Enums {
  MAIN = "/",
  COURSES = "/courses",
  COURSES_DETAILS = "/courses/:id",
  TEACHERS = "/teachers",
  TEACHERS_DETAILS = "/teachers/:id",
  STUDENTS = "/students",
  STUDENTS_DETAILS = "/students/:id",
}
